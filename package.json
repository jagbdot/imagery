{
  "name": "imagery",
  "scripts": {
    "start": "parcel index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build index.html --no-minify --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel",
    "deploy": "gh-pages -d dist"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "homepage": "https://jagbdot.github.io/imagery/",
  "dependencies": {
    "dat.gui": "^0.7.6",
    "ev-emitter": "^1.1.1",
    "fontfaceobserver": "^2.1.0",
    "gh-pages": "^3.1.0",
    "gsap": "^3.0.5",
    "parcel": "^1.12.4",
    "three": "^0.112.1",
    "three-orbit-controls": "^82.1.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1"
  }
}
